<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="HomeWork.App.Views.MainPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:HomeWork.App.ViewsModels">
    <ContentPage.BindingContext>
        <local:MainPageModel />
    </ContentPage.BindingContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <Label Text="Show" HorizontalTextAlignment="Center" FontSize="14"/>

        <ListView ItemsSource="{Binding ShowBars}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <HorizontalStackLayout>
                        <Label Text="{Binding Name}" FontSize="15"/>
                        <BoxView Color="Azure"/>
                    </HorizontalStackLayout>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </Grid>
</ContentPage>
